--- a/docker/Dockerfile.rocm_base
+++ b/docker/Dockerfile.rocm_base
@@ -27,7 +27,7 @@
 ENV PATH=/opt/rocm/llvm/bin:/opt/rocm/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
 ENV ROCM_PATH=/opt/rocm
 ENV LD_LIBRARY_PATH=/opt/rocm/lib:/usr/local/lib:
-ARG PYTORCH_ROCM_ARCH=gfx90a;gfx942;gfx950;gfx1100;gfx1101;gfx1200;gfx1201;gfx1150;gfx1151
+ARG PYTORCH_ROCM_ARCH=gfx90a;gfx942;gfx950;gfx1030;gfx1100;gfx1101;gfx1200;gfx1201;gfx1150;gfx1151
 ENV PYTORCH_ROCM_ARCH=${PYTORCH_ROCM_ARCH}
 ENV AITER_ROCM_ARCH=gfx942;gfx950
 # Note: Do not set MORI_GPU_ARCHS here, it is automatically inferred at runtime
@@ -188,18 +188,21 @@
 
 ###
 ### MORI Build
+### (vllm-rdna2-recipe / patch 0005: MORI, FlashAttention and AITER are CDNA-only — FA's setup.py
+### asserts on gfx10xx — so each of their steps is skipped when PYTORCH_ROCM_ARCH has no gfx9xx arch.)
 ###
 FROM base AS build_mori
 ARG MORI_BRANCH
 ARG MORI_REPO
 RUN --mount=type=bind,from=build_pytorch,src=/app/install/,target=/install \
-    pip install /install/*.whl
-RUN git clone ${MORI_REPO}
-RUN cd mori \
+    if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then pip install /install/*.whl; fi
+RUN if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then git clone ${MORI_REPO}; fi
+RUN if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then cd mori \
     && git checkout ${MORI_BRANCH} \
     && git submodule update --init --recursive \
-    && python3 setup.py bdist_wheel --dist-dir=dist && ls /app/mori/dist/*.whl
-RUN mkdir -p /app/install && cp /app/mori/dist/*.whl /app/install
+    && python3 setup.py bdist_wheel --dist-dir=dist && ls /app/mori/dist/*.whl; \
+    else echo "MORI: skipped, no CDNA arch in PYTORCH_ROCM_ARCH=${PYTORCH_ROCM_ARCH}"; fi
+RUN mkdir -p /app/install && if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then cp /app/mori/dist/*.whl /app/install; fi
 
 
 ###
@@ -210,9 +213,9 @@
 ARG FA_REPO
 ARG USE_SCCACHE
 RUN --mount=type=bind,from=build_pytorch,src=/app/install/,target=/install \
-    pip install /install/*.whl
-RUN git clone ${FA_REPO}
-RUN cd flash-attention \
+    if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then pip install /install/*.whl; fi
+RUN if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then git clone ${FA_REPO}; fi
+RUN if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then cd flash-attention \
     && git checkout ${FA_BRANCH} \
     && git submodule update --init \
     && if [ "$USE_SCCACHE" = "1" ]; then \
@@ -220,8 +223,9 @@
            && sccache --show-stats; \
        fi \
     && GPU_ARCHS=$(echo ${PYTORCH_ROCM_ARCH} | sed -e 's/;gfx1[0-9]\{3\}//g') python3 setup.py bdist_wheel --dist-dir=dist \
-    && if [ "$USE_SCCACHE" = "1" ]; then sccache --show-stats; fi
-RUN mkdir -p /app/install && cp /app/flash-attention/dist/*.whl /app/install
+    && if [ "$USE_SCCACHE" = "1" ]; then sccache --show-stats; fi; \
+    else echo "FlashAttention: skipped, no CDNA arch in PYTORCH_ROCM_ARCH=${PYTORCH_ROCM_ARCH}"; fi
+RUN mkdir -p /app/install && if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then cp /app/flash-attention/dist/*.whl /app/install; fi
 
 
 ###
@@ -232,20 +236,21 @@
 ARG AITER_REPO
 ARG USE_SCCACHE
 RUN --mount=type=bind,from=build_pytorch,src=/app/install/,target=/install \
-    pip install /install/*.whl
-RUN git clone --recursive --branch ${AITER_BRANCH} ${AITER_REPO}
-RUN cd aiter \
+    if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then pip install /install/*.whl; fi
+RUN if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then git clone --recursive --branch ${AITER_BRANCH} ${AITER_REPO}; fi
+RUN if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then cd aiter \
     && git submodule update --init --recursive \
-    && pip install -r requirements.txt
-RUN pip install pyyaml && cd aiter \
+    && pip install -r requirements.txt; fi
+RUN if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then pip install pyyaml && cd aiter \
     && if [ "$USE_SCCACHE" = "1" ]; then \
            export HIP_CLANG_PATH=/opt/sccache-wrappers \
            && sccache --show-stats; \
        fi \
     && PREBUILD_KERNELS=1 AITER_USE_SYSTEM_TRITON=1 GPU_ARCHS=${AITER_ROCM_ARCH} python3 setup.py bdist_wheel --dist-dir=dist \
     && if [ "$USE_SCCACHE" = "1" ]; then sccache --show-stats; fi \
-    && ls /app/aiter/dist/*.whl
-RUN mkdir -p /app/install && cp /app/aiter/dist/*.whl /app/install
+    && ls /app/aiter/dist/*.whl; \
+    else echo "AITER: skipped, no CDNA arch in PYTORCH_ROCM_ARCH=${PYTORCH_ROCM_ARCH}"; fi
+RUN mkdir -p /app/install && if echo "${PYTORCH_ROCM_ARCH}" | grep -q 'gfx9'; then cp /app/aiter/dist/*.whl /app/install; fi
 
 
 ###
@@ -259,13 +264,13 @@
 RUN --mount=type=bind,from=build_triton,src=/app/install/,target=/install \
     cp /install/*.whl /app/debs
 RUN --mount=type=bind,from=build_fa,src=/app/install/,target=/install \
-    cp /install/*.whl /app/debs
+    find /install -name '*.whl' -exec cp {} /app/debs \;
 RUN --mount=type=bind,from=build_amdsmi,src=/app/install/,target=/install \
     cp /install/*.whl /app/debs
 RUN --mount=type=bind,from=build_pytorch,src=/app/install/,target=/install \
     cp /install/*.whl /app/debs
 RUN --mount=type=bind,from=build_aiter,src=/app/install/,target=/install \
-    cp /install/*.whl /app/debs
+    find /install -name '*.whl' -exec cp {} /app/debs \;
 
 # Full debs stage - includes Mori (used by Docker releases)
 FROM base AS debs
@@ -273,15 +278,15 @@
 RUN --mount=type=bind,from=build_triton,src=/app/install/,target=/install \
     cp /install/*.whl /app/debs
 RUN --mount=type=bind,from=build_fa,src=/app/install/,target=/install \
-    cp /install/*.whl /app/debs
+    find /install -name '*.whl' -exec cp {} /app/debs \;
 RUN --mount=type=bind,from=build_amdsmi,src=/app/install/,target=/install \
     cp /install/*.whl /app/debs
 RUN --mount=type=bind,from=build_pytorch,src=/app/install/,target=/install \
     cp /install/*.whl /app/debs
 RUN --mount=type=bind,from=build_aiter,src=/app/install/,target=/install \
-    cp /install/*.whl /app/debs
+    find /install -name '*.whl' -exec cp {} /app/debs \;
 RUN --mount=type=bind,from=build_mori,src=/app/install/,target=/install \
-    cp /install/*.whl /app/debs
+    find /install -name '*.whl' -exec cp {} /app/debs \;
 
 FROM base AS final
 RUN --mount=type=bind,from=debs,src=/app/debs,target=/install \
