# summary: 122B MoE — NOT turnkey; run via the repo wrapper (see BUILD.md)
PRESET_NAME="qwen35-122b-autoround"
PRESET_UNSUPPORTED="needs one-time weight conversions (convert.py + quantize_mtp.py), a PP=3 or TP=4 host layout with the platform-stability stack, the V2 runner env, a tuned MoE config, and its own TunableOp lm_head rows (tn_151936_*). Clone the repo and use builds/Intel-Qwen3.5-122B-A10B-int4-AutoRound/serve.sh or serve-4gpu.sh."
